#### Note

I am not able to delete the db file in the database folder for some reason but that is not the main file as the file is created in the root directory as per the path is root in var.

## Creating Virtual environment

Before beginning with the app, need to be able to create the virtual environment.

## Installing required dependencies

Download required dependencies from the requirement.txt

## After just need to run the app 

python app.py # for windows


## For the postman link there if using your own then please add "/api" to the url 

## Here is the complete link - http://127.0.0.1:5000/api/country/

### Need to register the key in the headers

X-API-KEY in key field and API key in values

## Docker

# Build and start the container

docker-compose up -d

# To view logs docker-compose logs -f


